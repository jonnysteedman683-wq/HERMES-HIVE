You are the lead front-end engineer on a specialist design team. Build ONE complete homepage for HERMES-HIVE — an autonomous multi-agent AI swarm that continuously improves 4 code repositories (HERMES-HIVE, GTDown, gtdown-obsidian, sentinel-main) with 9 AI agents per repo running in 90-minute cycles.

DELIVERABLE: a single self-contained file at C:/Users/jonny/HERMES-HIVE/homepage/variant-C/index.html — inline CSS and JS only, zero external dependencies, no CDNs, no network requests. Must render perfectly when opened via file:// and be responsive.

MANDATORY VISUAL SPEC:
1. A SPINNING HEXAGONAL ARCHITECTURE: a hexagonal lattice / nested hexagon rings that rotates slowly and continuously (never stops). The hexagons represent the swarm architecture — label or arrange cells to evoke the 4 repos at the core with an outer ring of 9 agent nodes.
2. GLOWING ORB PERSISTING WITHIN: a luminous orb lives INSIDE the hexagonal structure at all times — it never disappears. It pulses with a soft breathing glow, casting light onto nearby hex edges. Additional smaller orbs may drift between cells, but the central orb is permanent.
3. Dark, cosmic, premium aesthetic. Smooth 60fps animation — use canvas or SVG + requestAnimationFrame (no janky CSS-only loops for the rotation). No console errors.
4. Content overlay: title "HERMES-HIVE", tagline "Autonomous multi-repo AI swarm", a subtle live-status strip (static placeholders: 4 repos / 9 agents per cycle / 90-min heartbeat), and an "Enter the Hive" CTA button with hover glow.
5. Typography: elegant, precise, thin weights.

CREATIVE DIRECTION for your variant — TECHNICAL SCHEMATIC / HUD: holographic mission-control aesthetic. Emerald/teal phosphor glow, faint scanlines and grid backdrop, monospace micro-labels on hex edges (agent IDs, repo names, cycle counters ticking), occasional data-readout flickers. The spinning hex structure feels like a live tactical display of the swarm. The central orb is the reactor core — bright, contained, humming. Precise, sci-fi, operational.

When finished, verify the file exists, run a quick sanity check on the HTML structure, and report: absolute file path, animation techniques used, and any trade-offs.
